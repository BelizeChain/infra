---
apiVersion: v1
kind: Service
metadata:
  name: belizechain-node
  namespace: belizechain
  labels:
    app: belizechain-node
    app.kubernetes.io/name: belizechain-node
    app.kubernetes.io/component: blockchain
spec:
  type: ClusterIP
  ports:
    - name: p2p
      port: 30333
      targetPort: 30333
      protocol: TCP
    - name: rpc
      port: 9933
      targetPort: 9944
      protocol: TCP
    - name: ws
      port: 9944
      targetPort: 9944
      protocol: TCP
  selector:
    app: belizechain-node

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blockchain-data
  namespace: belizechain
  labels:
    app: belizechain-node
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
  storageClassName: managed-csi-premium

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: belizechain-node
  namespace: belizechain
  labels:
    app: belizechain-node
    app.kubernetes.io/name: belizechain-node
    app.kubernetes.io/component: blockchain
    app.kubernetes.io/part-of: belizechain-platform
spec:
  serviceName: belizechain-node
  replicas: 1
  selector:
    matchLabels:
      app: belizechain-node
  template:
    metadata:
      labels:
        app: belizechain-node
        app.kubernetes.io/name: belizechain-node
        app.kubernetes.io/component: blockchain
    spec:
      containers:
        - name: belizechain-node
          image: belizechainacr.azurecr.io/belizechain-node:latest
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsUser: 0
            runAsGroup: 0
          ports:
            - name: p2p
              containerPort: 30333
              protocol: TCP
            - name: rpc
              containerPort: 9933
              protocol: TCP
            - name: ws
              containerPort: 9944
              protocol: TCP
          env:
            - name: RUST_LOG
              valueFrom:
                configMapKeyRef:
                  name: belizechain-config
                  key: LOG_LEVEL
            - name: BELIZECHAIN_CHAIN
              valueFrom:
                configMapKeyRef:
                  name: belizechain-config
                  key: CHAIN
            - name: BELIZECHAIN_BASE_PATH
              value: "/data"
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          command:
            - /usr/local/bin/belizechain-node
            - --base-path
            - /data
            - --chain
            - dev
            - --port
            - "30333"
            - --rpc-port
            - "9944"
            - --unsafe-rpc-external
          volumeMounts:
            - name: blockchain-data
              mountPath: /data
          resources:
            requests:
              memory: "4Gi"
              cpu: "250m"
            limits:
              memory: "8Gi"
              cpu: "4000m"
          livenessProbe:
            tcpSocket:
              port: 9944
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 9944
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
  volumeClaimTemplates:
    - metadata:
        name: blockchain-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 100Gi
        storageClassName: managed-csi-premium
